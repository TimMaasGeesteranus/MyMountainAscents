<UserControl
    x:Class="MyMountainAscents.UWP.UserControls.MountainCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyMountainAscents.UWP.UserControls"
    xmlns:models="using:MyMountainAscents.UWP.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <StackPanel>
            <ListView ItemsSource="{x:Bind Mountains.MountainCollection.Mountains}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Mountain">
                        <TextBlock Text="{x:Bind Name}" />
                    </DataTemplate>

                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

    </Grid>
</UserControl>
